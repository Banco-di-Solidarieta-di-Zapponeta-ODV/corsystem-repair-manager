import DashboardClient from "./DashboardClient";

export const metadata = {
  title: "Dashboard operativo | CorSystem Repair Manager"
};

export default function DashboardOperativoPage() {
  return <DashboardClient />;
}
